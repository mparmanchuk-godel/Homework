Act as a Senior SDET fixing a utility function in JavaScript.

Context:
- File: src/utils/dateHelper.js
- Function currently breaks when input is null or undefined

Task:
- Add a guard to handle null/undefined input
- If input is null or undefined → return empty string ""
- Keep all other behavior unchanged

Strict requirements:
- Do NOT refactor the whole file
- Do NOT rename the function
- Do NOT change function signature
- Only update this function

Example:
- formatDate(null) → ""
- formatDate(undefined) → ""

Output:
- Return the full corrected file