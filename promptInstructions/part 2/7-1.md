Act as a Senior SDET working with JavaScript + Playwright using Page Object Model.

Context:
- Project structure:
  - tests/e2e/checkout.spec.js
  - src/pages/*
  - src/components/Header.js
  - src/fixtures/testData.js
- Base implementation already exists

Task:
1. Refactor Page Objects and test:
   - Ensure clean separation of responsibilities
   - Remove any raw selectors from test (only Page Object usage)
2. Integrate fixture data:
   - Use product name and expected total from `src/fixtures/testData.js`
3. Update test `checkout.spec.js`:

Flow:
- // Initialization: open search page
- // User actions: search, select product, add to cart
- // Verification: cart badge increments (use assertion, not waits)
- // User actions: proceed to checkout
- // Verification: total matches expected (from fixture)

Requirements:
- Keep methods consistent with existing framework style
- Avoid duplication in test
- Keep test readable as a business flow
- Do NOT introduce unnecessary abstractions

Output:
1. Updated Page Objects (if needed)
2. Updated `checkout.spec.js`
3. Short explanation of improvements