Act as a Senior SDET working with JavaScript + Playwright using Page Object Model.

Context:
- Project already has AuthPage, HomePage
- Fixture file exists: `src/fixtures/testData.js` with valid and invalid users
- Goal: positive and negative login tests

Task:
1. Import AuthPage and HomePage from `src/pages`
2. Import users from fixture
3. Update test `auth.spec.js`:
   - Positive login: use valid user, verify HomePage avatar() is visible
   - Negative login: use invalid user, verify AuthPage errorMessage() is visible
4. Structure test clearly:
   // Initialization
   // User actions
   // Verification
5. Remove duplicated hardcoded steps; use login(user, pass) method in AuthPage
6. Keep locators, conventions, and method patterns consistent with existing framework

Output:
1. Refactored AuthPage and HomePage (if needed)
2. Updated `auth.spec.js` test code
3. Short explanation of changes and why it is better