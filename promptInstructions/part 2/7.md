Act as a Senior SDET working with JavaScript + Playwright using Page Object Model.

Task:
- Create Page Objects:
  - SearchPage
  - ProductPage
  - CartPage
  - CheckoutPage
  - Header (component)
- Create a test file `checkout.spec.js`

Requirements:
- Use modern ES6+ syntax
- Follow Page Object Model structure
- Each class should include required methods:

SearchPage:
- queryInput()
- submit()
- productResult(name)

ProductPage:
- addToCart()
- title()
- price()

CartPage:
- items()
- proceedToCheckout()

CheckoutPage:
- total()
- placeOrder()

Header:
- cartBadge()

Test:
- Include full flow:
  // Initialization
  // User actions
  // Verification

Flow steps:
- open search page
- search for product
- select product
- add to cart
- verify cart badge increment
- proceed to checkout
- verify total

Important:
- Use realistic locators based on this DOM:
<div data-testid="cart-summary">
  <span data-testid="cart-total">$100</span>
  <button data-testid="checkout-btn">Checkout</button>
</div>

- Do NOT use fixtures yet — hardcode product name and price
- Keep some duplication in test steps (e.g., repeated selectors or actions)
- Do NOT optimize or refactor — keep it simple and slightly repetitive