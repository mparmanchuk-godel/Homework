Act as a Senior SDET working with JavaScript + Playwright using Page Object Model.

Context:
- Existing SearchPage and ResultsPage
- Existing test `search.spec.js` with naive verification

Task:
1. Refactor ResultsPage:
   - Add collection-based helper methods (e.g., getAllPrices())
   - Ensure methods return usable data (numbers, not raw strings)

2. Update test `search.spec.js`:
   - Replace index-based checks with full validation
   - Assert that ALL result prices satisfy the filter (< 1000)

3. Keep structure:
   // Initialization
   // User actions
   // Verification

Requirements:
- Use clean iteration (e.g., loop through all prices)
- Convert price strings (e.g., "$999") to numbers before comparison
- Do NOT use raw selectors in test
- Keep code readable and aligned with framework style
- Avoid over-engineering

Output:
1. Updated ResultsPage
2. Updated `search.spec.js`
3. Short explanation of improvements