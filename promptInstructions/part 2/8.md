Act as a Senior SDET working with JavaScript + Playwright using Page Object Model.

Task:
- Create Page Objects:
  - SearchPage
  - ResultsPage
- Create a test file `search.spec.js`

Requirements:
- Use modern ES6+ syntax
- Follow Page Object Model structure

SearchPage:
- queryInput()
- submit()
- applyFilter(filterName)

ResultsPage:
- items()
- titleOf(index)
- priceOf(index)

Test:
- Include flow:
  // Initialization
  // User actions
  // Verification

Flow steps:
- open search page
- type "Laptop"
- apply filter "Price < $1000"
- verify results

Important:
- Use realistic locators based on this DOM:
<div role="list" data-testid="results">
  <div data-testid="result-item">
    <span class="title">Laptop</span>
    <span class="price">$999</span>
  </div>
</div>

- Verification should be naive:
  - check only 1–2 items manually (by index)
  - DO NOT loop through all items
- Do NOT create collection helper methods (like getAllPrices)
- Keep some duplication in test (e.g., repeated calls to priceOf)
- Keep implementation simple and not optimized