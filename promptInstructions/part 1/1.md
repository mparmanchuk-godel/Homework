Act as a Senior SDET working with JavaScript and Playwright using the Page Object Model.

Create a sample Page Object class for a web page (e.g., LoginPage or ProductPage).

Requirements:
- Use modern JavaScript (ES6+)
- Use Playwright locators
- Follow Page Object Model best practices
- The class should include at least 3 public methods

Important:
- Two of the methods MUST be very similar (duplicate logic with only small differences)
- The third method should be different

Example idea (but do not copy exactly):
- fillUsername()
- fillPassword()
- clickLogin()

The duplicated methods should be good candidates for refactoring into one parameterized method later.

Keep the code realistic, clean, and not overly long.