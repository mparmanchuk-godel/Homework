Act as a Senior SDET working with JavaScript + Playwright using the Page Object Model.

Task:
- Create a minimal Page Object class or component representing a new UI element (toggle, checkbox, or dropdown).
- Create one test file that interacts with this element.
- Include realistic locators and a simple interaction (e.g., toggle on/off, select dropdown value).

Requirements:
- Use modern ES6+ syntax
- Follow the existing Page Object conventions
- Methods must be simple and self-contained
- Use // path: headers to indicate repo structure
- Add comments in test: // Initialization, // User actions, // Verification
- Do NOT integrate with existing Page Objects or tests yet — keep standalone