Act as a Senior SDET working with JavaScript and Playwright using the Page Object Model.

Create:
1. One Page Object class
2. One or two test files that use this Page Object

Requirements:
- Use modern JavaScript (ES6+)
- Use Playwright test runner
- Keep the code realistic but not слишком длинным

Important:
- The Page Object should simulate a recent refactor:
  - Rename or merge at least one method
  - Remove or change method signatures
- The test files should still use the OLD method names (outdated references)

Goal:
- Introduce clear inconsistencies between Page Object and tests
- Tests should look valid but contain broken method calls due to refactor

Do NOT fix anything — intentionally leave mismatches for later analysis.