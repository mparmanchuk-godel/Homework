Act as a Senior SDET working with JavaScript and Playwright.

Create a sample test file using Playwright test runner.

Requirements:
- Use modern JavaScript (ES6+)
- Include at least 3 test cases
- Use a Page Object (you can assume it already exists, e.g., LoginPage or ProductPage)

Important:
- At least 2 tests MUST contain repeated sequences of actions (same steps duplicated)
- The repeated steps should be realistic (e.g., login flow, adding product, navigation, etc.)
- Do NOT extract helpers yet — keep duplication

Keep the code clean, readable, and realistic, but intentionally include duplication for later refactoring.