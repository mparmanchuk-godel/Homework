Act as a Senior SDET working with JavaScript + Playwright using the Page Object Model.

Context:
- Your project already has existing Page Objects and tests
- A new UI element has been introduced (toggle, checkbox, dropdown)
- You have the initial standalone code for this element

Task:
1. Extend an existing Page Object by adding a new method for the new element.
2. Update one of your existing tests to interact with this element.